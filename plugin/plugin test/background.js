
/*function getLastKey(){
    var req = new XMLHttpRequest();
    req.open('GET', 'http://localhost:8080/HelloWorld/resources/publicEncryptionKeys.txt'   /*'https://ease.space/resources/websites/Gmail/connect.json'*/, false);
    /*req.send(null);
    if(req.status == 200){
        var keys = req.responseText.split("\n");
        var lastKey = keys[keys.length-1];
        return {date:lastKey.substring(0,6), key:lastKey.substring(8,lastKey.length)};
    } else {
        return "error";
    }
}*/


